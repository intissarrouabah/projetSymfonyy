<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
<h1><a class="navbar-brand" href="{{ path('event_index') }}">Sortir.Com</a></h1>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        {# TODO pour l'instant j'ai mis ces conditions pour pouvoir continuer de travailler, mais il faudra le gérer autrement#}
        <ul class="navbar-nav">
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('city_index') }}">Villes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('campus_index') }}">Campus</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_USER') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('event_index') }}">Sorties</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('event_index') }}">Sorties</a>
                </li>
            {% endif %}
            {% if is_granted('ROLE_USER') %}
           <li class="nav-item">
                <a class="nav-link" href="{{ path('profile_index') }}">Mon profil</a>
            </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('profile_index') }}">Mon profil</a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_USER') %}
                <li class="nav-item">
                <a class="nav-link" href="{{ path('logout') }}">Se déconnecter</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('logout') }}">Se déconnecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>