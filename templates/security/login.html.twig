{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        .error {color: red}
    </style>

    <div class="example-wrapper">


        <h1>Connexion! ✅</h1>
        {#ici on va faire les messages d'erreurs#}
        {# TODO traduire les messages d'erreurs   #}
        {% if error %}
            <span class="error">{{ error.messageKey }}</span>
        {% endif %}

        <form action="{{ path('login') }}" method="POST">
            <input type="text" name="_username" value="{{ last_username }}" placeholder="Pseudo...">
            <input type="password" name="_password" placeholder="Mot de passe ..." >

            <button type="submit">Connexion</button>


        </form>

        <a href="{{ path('participant_new') }}">Inscription</a>


    </div>
{% endblock %}
