{% extends 'base.html.twig' %}

{% block title %}Campus{% endblock %}

{% block body %}
    <h1>Campus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Identifiant</th>
                <td>{{ campus.id }}</td>
            </tr>
            <tr>
                <th>Campus</th>
                <td>{{ campus.nameCampus }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('campus_index') }}">Retour a la liste</a>

    <a href="{{ path('campus_edit', {'id': campus.id}) }}">Modifier</a>

    {{ include('campus/_delete_form.html.twig') }}
{% endblock %}
